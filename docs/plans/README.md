# docs/plans/

Implementation plans and specifications. Files here are structured markdown documents describing planned work.

Lifecycle: plans are created during planning, consumed during implementation, and deleted after the sashay completes (see `~/.agents/agents-md-detail/workflow-rituals/sashay.md` for deletion criteria).
